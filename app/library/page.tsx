import type { Metadata } from 'next';
import Link from 'next/link';
import { cabinets, libraryFiles } from './content';

export const metadata: Metadata = { title: 'Roofing Library', description: 'A cabinet-style roofing knowledge library with dedicated files for inspections, roof systems, storm documentation, estates, and commercial roofing.' };

export default function LibraryPage() {
  return <main className="library-page"><section className="library-hero"><div className="shell"><div><p className="eyebrow light">THE COWBOY ROOFING LIBRARY</p><h1>A warehouse<br/>of useful files.</h1></div><aside><small>FACILITY MAP</small><span><b>04</b>CABINETS</span><span><b>{libraryFiles.length}</b>TECHNICAL FILES</span><span><b>01</b>CLEAR NEXT STEP</span></aside></div></section><section className="cabinet-map shell"><header><p className="eyebrow">CABINET → FOLDER → FILE</p><h2>Open what you need.<br/>Keep the rest organized.</h2><p>The site works like a physical records facility. Each cabinet covers a major roofing discipline. Every folder opens its own detailed page.</p></header>{cabinets.map((cabinet, cabinetIndex) => <section className="file-cabinet" key={cabinet}><header><span>CABINET 0{cabinetIndex + 1}</span><h2>{cabinet}</h2><small>{libraryFiles.filter((file) => file.cabinet === cabinet).length} FILES</small></header><div>{libraryFiles.filter((file) => file.cabinet === cabinet).map((file) => <Link href={`/library/${file.slug}`} key={file.slug}><span className="folder-tab">{file.code}</span><small>TECHNICAL FILE</small><h3>{file.title}</h3><p>{file.subtitle}</p><b>OPEN FILE →</b></Link>)}</div></section>)}</section><section className="library-routing"><div className="shell"><div><p className="eyebrow light">DON’T KNOW THE FILE?</p><h2>Let the roof route you.</h2><p>Answer six roof questions and the advisor will point you toward the most useful system and next action.</p></div><div><Link href="/roof-advisor">RUN THE ROOF ADVISOR →</Link><Link href="/free-inspection">REQUEST AN INSPECTION →</Link></div></div></section></main>;
}
